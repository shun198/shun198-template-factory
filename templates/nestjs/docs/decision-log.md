# Decision Log

## Initial Choices

- Kept the starter close to the NestJS standard layout to reduce surprise.
- Added only a config service and health endpoint as baseline features.
- Used Jest because it is the default testing path for NestJS.
