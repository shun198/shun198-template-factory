# Tests

Use `make validate` and `make lint` as the baseline infrastructure checks for this starter.

Run them per environment when needed:

- `make TF_DIR=envs/dev validate`
- `make TF_DIR=envs/prod validate`
