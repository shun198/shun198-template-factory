# Decision Log

## Initial Choices

- Used Cloud Storage as the sample resource because it keeps the template accessible while still being real infrastructure.
- Split environment roots from reusable modules to support growth without over-designing the starter.
- Kept backend config as an example file so the template stays safe to clone.
