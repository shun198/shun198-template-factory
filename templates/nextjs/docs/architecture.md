# Architecture

- `app/layout.tsx` defines the shared shell.
- `app/page.tsx` provides a small sample dashboard home.
- `components/` holds reusable UI building blocks.
- `tests/` contains page-level tests.
