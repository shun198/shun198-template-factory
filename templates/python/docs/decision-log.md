# Decision Log

## Initial Choices

- Used `typer` for a readable CLI that can grow into multiple commands.
- Kept runtime wiring in a small number of modules so the starter stays easy to understand.
- Chose `ruff` for both linting and formatting to keep setup simple.
